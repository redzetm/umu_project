.\" umu_head(1) manual page
.\" Date: 2026-01-02
.TH UMU_HEAD 1 "2026-01-02" "UmuOS" "ユーザーコマンド"
.SH 名前
umu_head \- 入力の先頭 N 行を出力する
.SH 書式
.B umu_head
.RI [ -n\ N ]
.RI [ -N ]
.RI [ FILE ...]
.SH 説明
.B umu_head
は、入力の先頭 N 行を標準出力へ出力します。
N を省略した場合は 10 行です。
.PP
引数がない場合は標準入力を読み込みます。
ファイル名に
.B -
を指定した場合、その位置で標準入力を読み込みます。
.PP
この実装は学習・解析用途の最小版であり、一般的な\fBhead\fPが持つ
バイト数指定（\fB-c\fP）やヘッダ表示などの機能は実装していません。
.SH オプション
.TP
.BI -n\  N
先頭 N 行を出力します（デフォルト 10）。
.TP
.BI -N
\fB-n N\fP の短縮形です（例: \fB-1\fP は先頭 1 行）。
.SH 例
.TP
.B umu_head file.txt
file.txt の先頭 10 行を出力します。
.TP
.B umu_head -n 3 file.txt
file.txt の先頭 3 行を出力します。
.TP
.B umu_head -n 5 - file.txt
標準入力の先頭 5 行を出力した後、file.txt の先頭 5 行を出力します。
.SH 終了ステータス
.TP
0
すべての入力を正常に処理できました。
.TP
1
読み取りまたは書き込みでエラーが発生しました。
.SH 注意
`-c` や `-q`/`-v`、`--help`/`--version` は実装していません。
.SH 関連項目
.BR tail (1),
.BR sed (1)
